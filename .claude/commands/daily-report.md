---
description: Write or update today's day-by-day status file in docs/progress/.
---

Run the `progress-log` skill to write or update today's
`docs/progress/YYYY-MM-DD.md`. It will itself run `gap-analysis` and
`quality-gate` first so the report reflects current, verified state —
don't skip those steps or write the file from memory of earlier
conversation turns.

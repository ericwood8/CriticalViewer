---
description: Report what's built vs. missing against the kickoff brief's 4 features, optionally scoped to one feature.
---

Run the `gap-analysis` skill.

Feature scope: $ARGUMENTS

If a feature name was given above, scope the analysis to just that
feature. If it's empty, analyze all 4 features from `CLAUDE.md`.
